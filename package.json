{
  "name": "pi-inline-format-extensions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "workspaces": [
    "packages/*"
  ],
  "pi": {
    "extensions": [
      "./packages/host/extensions/index.ts"
    ]
  },
  "scripts": {
    "build:pages-demo": "tsx scripts/build-pages-demo.ts",
    "check": "npm run format && npm run typecheck && npm run check:root-package-install",
    "check:root-package-install": "node ./scripts/check-root-package-install.mjs",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.63.1",
    "@types/node": "^25.5.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
